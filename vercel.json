{
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "functions": {
    "api/**/*.js": {
      "maxDuration": 10
    }
  },
  "rewrites": [
    { "source": "/r/:id", "destination": "/r/[id]" },
    { "source": "/(.*)", "destination": "/" }
  ]
}